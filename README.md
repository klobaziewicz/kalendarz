# kalendarz
Projekt zrobiony przy użyciu HTML, CSS, PHP, bazy danych MySQL. Aby projekt działał należy odpalić na localhost oraz stworzyć w MySQL bazę danych o nazwie: wakacje oraz tabelę o nazwie: todo z 4 kolumnami: Id (Int AI), nazwa(text), text(varchar 90) oraz data(date).
